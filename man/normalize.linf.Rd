% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linf.R
\name{normalize.linf}
\alias{normalize.linf}
\title{L-infinity normalization (row-wise divide by the per-sample maximum)}
\usage{
normalize.linf(S.counts)
}
\arguments{
\item{S.counts}{Numeric matrix (samples x features), nonnegative.}
}
\value{
Numeric matrix of the same size; each nonzero row has max == 1.
}
\description{
For each sample (row), divides all entries by that row's maximum so the
maximum relative abundance equals 1. Rows that are all zero remain all zero.
}
\examples{
S <- rbind(
  c(10, 5, 0),
  c(0, 0, 0),
  c(1, 2, 4)
)
Z <- normalize.linf(S)
apply(Z, 1, max)          # 1, 0, 1
Z[2, ]                     # stays all zeros

}
